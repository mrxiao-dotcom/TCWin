# 🔧 三大问题修复方案

## 📋 问题总结

用户反映的三个关键问题：

1. **基础配置还是存在被覆盖的问题**
2. **在修改状态窗口中，修改推仓的状态，保存后数据没有保存**
3. **如果基础配置中：保本没有启动，合约对应的状态配置里，就不应该出现，双击编时也不要可编辑**

## 🔍 问题根因分析

### 问题1：基础配置仍被覆盖

**根本原因**：虽然修复了 `MainViewModel.AutoMonitor.cs`，但还有其他地方在使用旧的保存服务

**发现的问题代码**：
- `Views/AutoMonitorConfigWindowSimple.xaml.cs` 第3061行：仍在使用 `SaveSingleAccountConfig`
- `Views/SimpleConfigEditorWindow.xaml.cs`：使用了 `_enhancedConfigManager.SaveConfiguration`
- `Services/EnhancedBaseConfigManager.cs`：可能与 `BaseConfigManager` 冲突

### 问题2：状态修改保存失败

**根本原因**：状态修改窗口的保存逻辑不完整
- 保存到了内存模型，但没有持久化到统一状态文件
- 可能存在数据源不一致问题

### 问题3：基础配置启用状态控制缺失

**根本原因**：界面没有根据基础配置的 `IsEnabled` 状态来控制合约状态的显示和编辑
- 需要在生成合约配置时过滤掉未启用的功能
- 需要在编辑界面禁用未启用的功能

## 🔧 修复方案

### 修复1：彻底统一基础配置保存

#### 1.1 修复 AutoMonitorConfigWindowSimple.xaml.cs

```csharp
// 修复前（第3061行）
configPersistenceService.SaveSingleAccountConfig(_mainViewModel.SelectedAccount.Name, _currentConfig);

// 修复后
BaseConfigManager.Instance.AddConfiguration(_currentConfig);
```

#### 1.2 修复 SimpleConfigEditorWindow.xaml.cs

```csharp
// 修复前
_enhancedConfigManager.SaveConfiguration(_selectedConfig);

// 修复后
BaseConfigManager.Instance.UpdateConfiguration(_selectedConfig);
```

#### 1.3 停用 EnhancedBaseConfigManager

```csharp
// 在 ServiceConfiguration.cs 中注释掉冲突的服务
// services.AddSingleton<EnhancedBaseConfigManager>();
```

### 修复2：完善状态修改保存逻辑

#### 2.1 修复状态窗口保存

在 `ContractConfigEditDialog.xaml.cs` 中确保保存后刷新统一状态文件：

```csharp
private void SaveContractConfigToFile()
{
    // 现有保存逻辑...
    
    // 🔧 新增：保存后刷新统一状态文件
    try
    {
        var stateService = App.ServiceProvider?.GetService<ContractMonitoringStateService>();
        if (stateService != null)
        {
            _ = Task.Run(async () =>
            {
                await stateService.SaveStateAsync(contractKey, updatedState);
                _logger?.LogInformation($"✅ 状态修改已同步到统一状态文件: {contractKey}");
            });
        }
    }
    catch (Exception ex)
    {
        _logger?.LogError(ex, "❌ 同步状态到统一状态文件失败");
    }
}
```

#### 2.2 加强状态修改日志

```csharp
// 在保存前后添加详细日志
_logger?.LogCritical($"🔧 保存前状态: {_editedConfig.BreakEvenStatus}");
// ... 保存逻辑 ...
_logger?.LogCritical($"🔧 保存后状态: 已持久化到文件");
```

### 修复3：实现基础配置启用状态控制

#### 3.1 合约配置生成时过滤

在生成合约配置时，只包含已启用的功能：

```csharp
private ContractMonitorModel GenerateContractConfigFromBaseConfig(PositionInfo position, AutoMonitorConfig baseConfig)
{
    var config = new ContractMonitorModel
    {
        // 基础信息...
    };
    
    // 🔧 只在基础配置启用时才添加保本配置
    if (baseConfig.BreakEvenConfig.IsEnabled)
    {
        config.BreakEvenTarget = baseConfig.BreakEvenConfig.TriggerProfitAmount;
        config.BreakEvenStatus = "-";
        // ... 其他保本相关属性
    }
    else
    {
        // 保本未启用时，设置为空或隐藏
        config.BreakEvenTarget = 0;
        config.BreakEvenStatus = ""; // 空字符串表示不显示
    }
    
    // 🔧 只在基础配置启用时才添加推仓配置
    if (baseConfig.AddPositionConfig.IsEnabled)
    {
        // 添加推仓相关配置...
    }
    
    // 🔧 只在基础配置启用时才添加保盈配置
    if (baseConfig.ProfitProtectionConfig.IsEnabled)
    {
        // 添加保盈相关配置...
    }
    
    return config;
}
```

#### 3.2 界面显示控制

在合约编辑窗口中，根据基础配置控制显示：

```csharp
private void LoadContractConfig(ContractMonitorModel config)
{
    // 获取基础配置
    var baseConfig = BaseConfigManager.Instance.GetConfiguration(config.BaseConfigName);
    
    if (baseConfig != null)
    {
        // 🔧 控制保本相关控件的显示和编辑
        var breakEvenEnabled = baseConfig.BreakEvenConfig.IsEnabled;
        BreakEvenPanel.Visibility = breakEvenEnabled ? Visibility.Visible : Visibility.Collapsed;
        BreakEvenStatusControl.IsEnabled = breakEvenEnabled;
        
        // 🔧 控制推仓相关控件的显示和编辑
        var addPositionEnabled = baseConfig.AddPositionConfig.IsEnabled;
        PushTierPanel.Visibility = addPositionEnabled ? Visibility.Visible : Visibility.Collapsed;
        
        // 🔧 控制保盈相关控件的显示和编辑
        var profitProtectionEnabled = baseConfig.ProfitProtectionConfig.IsEnabled;
        ProfitTierPanel.Visibility = profitProtectionEnabled ? Visibility.Visible : Visibility.Collapsed;
    }
}
```

#### 3.3 DataGrid 列显示控制

在合约列表中，动态控制列的显示：

```csharp
private void UpdateColumnVisibility()
{
    // 获取当前基础配置
    var baseConfig = GetCurrentBaseConfig();
    
    if (baseConfig != null)
    {
        // 🔧 控制保本列的显示
        BreakEvenColumn.Visibility = baseConfig.BreakEvenConfig.IsEnabled ? 
            Visibility.Visible : Visibility.Collapsed;
            
        // 🔧 控制推仓列的显示
        PushTier1Column.Visibility = baseConfig.AddPositionConfig.IsEnabled ? 
            Visibility.Visible : Visibility.Collapsed;
        PushTier2Column.Visibility = baseConfig.AddPositionConfig.IsEnabled ? 
            Visibility.Visible : Visibility.Collapsed;
        // ... 其他推仓列
        
        // 🔧 控制保盈列的显示
        ProfitTier1Column.Visibility = baseConfig.ProfitProtectionConfig.IsEnabled ? 
            Visibility.Visible : Visibility.Collapsed;
        // ... 其他保盈列
    }
}
```

## 📋 实施计划

### 阶段1：基础配置保存统一化（优先级：高）
1. 修复 `AutoMonitorConfigWindowSimple.xaml.cs`
2. 修复 `SimpleConfigEditorWindow.xaml.cs`
3. 停用冲突的配置管理器
4. 验证基础配置不再被覆盖

### 阶段2：状态修改保存修复（优先级：高）
1. 增强状态修改窗口的保存逻辑
2. 确保保存后同步到统一状态文件
3. 添加详细的保存日志
4. 验证状态修改能正确保存

### 阶段3：基础配置启用状态控制（优先级：中）
1. 修改合约配置生成逻辑
2. 实现界面显示控制
3. 实现编辑权限控制
4. 验证功能启用状态正确反映在界面上

## 🧪 验证方法

### 验证1：基础配置保存
1. 创建或修改基础配置
2. 重启程序
3. 确认配置没有被覆盖

### 验证2：状态修改保存
1. 打开合约状态编辑窗口
2. 修改推仓状态
3. 保存并关闭
4. 重新打开，确认修改已保存

### 验证3：基础配置启用状态控制
1. 在基础配置中禁用保本功能
2. 查看合约配置列表
3. 确认保本相关列不显示
4. 双击编辑，确认保本控件不可编辑

## 🎯 预期效果

- ✅ 基础配置完全不会被覆盖
- ✅ 状态修改能正确保存和持久化
- ✅ 界面根据基础配置动态显示和控制编辑权限
- ✅ 用户体验更加一致和直观 