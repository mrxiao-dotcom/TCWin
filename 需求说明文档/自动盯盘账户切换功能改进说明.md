# 自动盯盘账户切换功能改进说明

## 改进概述

根据用户需求，对自动盯盘功能进行了重要改进，使其能够正确处理账户切换场景。现在每个账户都有独立的自动盯盘配置，切换账户时会自动停止当前的自动盯盘功能，确保安全性和正确性。

## 核心改进

### 1. 账户独立配置存储

**改进前**：
- 所有账户共享一个自动盯盘配置
- 切换账户后配置仍然存在，可能导致错误操作

**改进后**：
```csharp
// 每个账户独立的自动盯盘配置字典
private readonly Dictionary<string, AutoMonitorConfig> _accountAutoMonitorConfigs = new();
```

**技术实现**：
- 使用`Dictionary<string, AutoMonitorConfig>`存储每个账户的配置
- 键为账户名称，值为该账户的自动盯盘配置
- 账户切换时自动加载对应的配置

### 2. 账户切换时自动停止功能

**问题分析**：
用户反馈切换账户后，自动盯盘功能还在运行，但实际上应该只监控当前活跃账户的持仓。如果不停止，可能会：
- 监控错误账户的持仓数据
- 执行不正确的交易操作
- 造成资金安全风险

**解决方案**：
在`OnSelectedAccountChanged`方法中增加自动停止逻辑：

```csharp
// 🛑 切换账户时自动停止自动盯盘功能
if (IsAutoMonitorRunning)
{
    _logger.LogInformation("检测到账户切换，自动停止自动盯盘功能");
    _ = Task.Run(async () =>
    {
        try
        {
            await StopAutoMonitorAsync(clearConfig: true);
            _logger.LogInformation("账户切换时自动停止自动盯盘成功");
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "账户切换时停止自动盯盘失败");
        }
    });
}
```

### 3. 配置加载和保存机制优化

#### 启动时配置加载
```csharp
// 从当前账户的配置中加载设置
if (SelectedAccount != null)
{
    if (_accountAutoMonitorConfigs.TryGetValue(SelectedAccount.Name, out var accountConfig))
    {
        _logger.LogInformation($"从账户 {SelectedAccount.Name} 加载现有配置...");
        configDialog.SetConfig(accountConfig);
        _currentAutoMonitorConfig = accountConfig;
    }
    else
    {
        _logger.LogInformation($"账户 {SelectedAccount.Name} 没有现有配置，使用默认配置");
    }
}
```

#### 配置保存机制
```csharp
// 保存配置到当前账户
_currentAutoMonitorConfig = configDialog.ConfigResult;
if (SelectedAccount != null)
{
    _accountAutoMonitorConfigs[SelectedAccount.Name] = _currentAutoMonitorConfig;
    _logger.LogInformation($"配置已保存到账户 {SelectedAccount.Name}: {_currentAutoMonitorConfig.Name}");
}
```

### 4. 停止方法参数化改进

**方法签名优化**：
```csharp
/// <summary>
/// 停止自动监控
/// </summary>
/// <param name="clearConfig">是否清空当前配置（账户切换时为true，用户手动停止时为false）</param>
private async Task StopAutoMonitorAsync(bool clearConfig = false)
```

**区分停止场景**：
- **用户手动停止**：`clearConfig = false`，保留配置，下次启动时使用相同配置
- **账户切换停止**：`clearConfig = true`，清空当前配置，避免配置混乱

## 用户体验改进

### 1. 安全性提升
- **防止误操作**：切换账户时自动停止，避免对错误账户执行操作
- **数据隔离**：每个账户的配置独立存储，不会相互影响
- **状态一致性**：UI状态与实际运行状态保持一致

### 2. 操作便捷性
- **配置记忆**：每个账户的配置会被记住，切换回来时无需重新配置
- **自动提示**：切换账户时会有日志提示，用户了解发生了什么
- **重新启动简单**：用户只需点击"自动盯盘"按钮即可重新启动

### 3. 状态反馈
- **按钮状态**：切换账户后按钮自动变为"自动盯盘"状态
- **状态消息**：显示"未启动"，明确当前状态
- **日志记录**：详细记录所有操作，便于调试和监控

## 工作流程

### 账户A启动自动盯盘
1. 用户选择账户A
2. 点击"自动盯盘"按钮
3. 配置自动盯盘参数
4. 启动监控，监控账户A的持仓

### 切换到账户B
1. 用户在下拉框中选择账户B
2. 系统自动检测到账户切换
3. 自动停止账户A的自动盯盘
4. 清空当前配置，重置UI状态
5. 加载账户B的数据

### 账户B重新启动自动盯盘
1. 用户点击"自动盯盘"按钮
2. 系统检查是否有账户B的历史配置
3. 如有历史配置，自动加载；如无，使用默认配置
4. 用户确认配置后启动
5. 开始监控账户B的持仓

## 技术细节

### 配置存储结构
```csharp
Dictionary<string, AutoMonitorConfig> _accountAutoMonitorConfigs
├── "账户A" → AutoMonitorConfig { ... }
├── "账户B" → AutoMonitorConfig { ... }
└── "账户C" → AutoMonitorConfig { ... }
```

### 状态管理
- **IsAutoMonitorRunning**：当前是否正在运行
- **_currentAutoMonitorConfig**：当前活跃的配置
- **_accountAutoMonitorConfigs**：所有账户的配置字典

### 异常处理
- 账户切换时的停止操作使用`Task.Run`异步执行，避免阻塞UI
- 所有操作都有完整的异常捕获和日志记录
- 失败时会记录错误日志，但不会影响账户切换的正常流程

## 测试验证

### 1. 功能测试
- [x] 账户A启动自动盯盘，切换到账户B时自动停止
- [x] 账户B重新启动自动盯盘，使用独立配置
- [x] 切换回账户A时，能够加载之前的配置
- [x] 清空账户选择时也会停止自动盯盘

### 2. 安全性测试
- [x] 确认切换账户时不会对错误账户执行操作
- [x] 确认配置不会在账户间混乱
- [x] 确认UI状态与实际运行状态一致

### 3. 用户体验测试
- [x] 切换账户时有明确的状态反馈
- [x] 重新启动自动盯盘操作简单直观
- [x] 历史配置能够正确加载和使用

## 总结

这次改进显著提升了自动盯盘功能的安全性和易用性：

1. **安全性**：账户切换时自动停止，防止误操作
2. **独立性**：每个账户有独立配置，互不干扰
3. **便捷性**：配置会被记住，重新启动简单
4. **可靠性**：完整的异常处理和状态管理

用户现在可以安全地在多个账户间切换，每个账户的自动盯盘配置都会被妥善管理，真正实现了"每个配置只跟当前选择账户挂钩"的需求。 