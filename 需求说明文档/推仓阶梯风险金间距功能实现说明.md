# 推仓阶梯风险金间距功能实现说明

## 功能概述

根据用户需求，将推仓阶梯的默认间距从固定100U改为基于账户风险金计算的智能间距。这样使得阶梯设置更符合个人的风险管理策略和账户规模。

## 修改内容

### 1. 增加推仓阶梯的智能间距

**修改前**：
- 固定增加100U间距
- 不考虑账户规模
- 所有用户使用相同的间距

**修改后**：
- 基于一份风险金计算间距
- 根据账户权益和风险次数动态调整
- 个性化的阶梯设置

### 2. 计算公式

```csharp
// 一份风险金 = 账户权益 ÷ 风险次数
var singleRiskCapital = _accountEquity / _riskCapitalTimes;
var riskCapitalIncrement = Math.Round(singleRiskCapital, 0); // 四舍五入到整数

// 新阶梯触发值 = 上一个阶梯触发值 + 一份风险金
var newTriggerAmount = lastStage.TriggerProfitAmount + riskCapitalIncrement;
```

### 3. 实际示例

#### 示例1：小资金账户
- 账户权益：1000U
- 风险次数：10次
- 一份风险金：1000 ÷ 10 = 100U
- 阶梯设置：100U, 200U, 300U, 400U, 500U...

#### 示例2：中等资金账户  
- 账户权益：5000U
- 风险次数：10次
- 一份风险金：5000 ÷ 10 = 500U
- 阶梯设置：500U, 1000U, 1500U, 2000U, 2500U...

#### 示例3：大资金账户
- 账户权益：20000U
- 风险次数：8次
- 一份风险金：20000 ÷ 8 = 2500U
- 阶梯设置：2500U, 5000U, 7500U, 10000U, 12500U...

## 技术实现

### 1. 修改增加阶梯按钮逻辑

```csharp
private void AddPositionStageButton_Click(object sender, RoutedEventArgs e)
{
    // 🔧 修改：使用一份风险金作为默认间距，而不是固定100U
    var singleRiskCapital = _accountEquity / _riskCapitalTimes; // 计算一份风险金
    var riskCapitalIncrement = Math.Round(singleRiskCapital, 0); // 四舍五入到整数
    
    // 智能计算新阶梯的默认值
    var newTriggerAmount = lastStage != null 
        ? lastStage.TriggerProfitAmount + riskCapitalIncrement 
        : riskCapitalIncrement; // 第一个阶梯就是一份风险金
}
```

### 2. 修改默认阶梯初始化

```csharp
private void InitializeStageDefaults()
{
    // 🔧 修改：使用风险金计算默认阶梯值
    var singleRiskCapital = _accountEquity / _riskCapitalTimes;
    var riskCapitalIncrement = Math.Round(singleRiskCapital, 0);
    
    // 推仓阶梯：1倍、2倍、3倍、4倍风险金
    // 阶梯1: 1 × 风险金
    // 阶梯2: 2 × 风险金  
    // 阶梯3: 3 × 风险金
    // 阶梯4: 4 × 风险金
}
```

### 3. 用户提示信息

当用户点击"增加推仓阶梯"时，系统会显示详细的计算信息：

```
已添加推仓阶梯5：
触发值：1250U
(基于一份风险金 1250U = 账户权益10000U ÷ 风险次数8)
```

## 界面优化

### 1. 智能提示
- 显示计算过程和结果
- 帮助用户理解阶梯设置逻辑
- 提供透明的计算依据

### 2. 动态适应
- 根据不同账户规模自动调整
- 保持阶梯间距的合理性
- 符合风险管理原则

## 优势分析

### 1. 个性化风险管理
- **小账户**：较小的阶梯间距，更精细的控制
- **大账户**：较大的阶梯间距，避免过度频繁操作
- **风险一致性**：每个阶梯都代表相同的风险级别

### 2. 符合交易逻辑
- 基于账户规模的比例设置
- 风险分散更加均匀
- 避免固定金额的局限性

### 3. 动态适应性
- 账户权益变化时，新增阶梯自动适应
- 风险次数调整时，计算自动更新
- 始终保持合理的风险比例

## 保盈止损阶梯优化

同时优化了保盈止损阶梯的默认设置：

```csharp
// 保盈止损基础值 = 风险金 × 10倍
var profitProtectionBase = riskCapitalIncrement * 10;

// 阶梯设置：
// 阶梯1: 10倍风险金时保护80%
// 阶梯2: 20倍风险金时保护80%  
// 阶梯3: 30倍风险金时保护80%
```

### 逻辑说明
- 保盈止损触发值设为风险金的10倍、20倍、30倍
- 这样确保只有在获得足够利润后才触发保护
- 保护金额为触发值的80%，保留20%继续获利空间

## 使用指南

### 1. 首次使用
1. 确保账户配置中的账户权益和风险次数设置正确
2. 打开自动盯盘配置对话框
3. 系统会根据您的设置自动计算合理的阶梯值

### 2. 增加阶梯
1. 点击"+ 增加推仓阶梯"按钮
2. 系统自动计算下一个阶梯的触发值
3. 查看弹出的提示信息，了解计算依据
4. 可以手动调整计算出的值

### 3. 参数调整建议
- **保守策略**：风险次数设置较大（如15-20次）
- **激进策略**：风险次数设置较小（如5-8次）
- **平衡策略**：风险次数设置中等（如8-12次）

## 兼容性说明

### 向后兼容
- 现有的手动设置的阶梯不受影响
- 只影响新增阶梯的默认值计算
- 用户仍可手动修改任何阶梯的参数

### 智能检测
- 系统会检测当前账户信息
- 如果无法获取账户信息，使用默认值（1000U权益，10次风险）
- 确保功能在任何情况下都能正常工作

## 修改的文件

### 核心修改：`Views/AutoMonitorConfigDialog.xaml.cs`

1. **AddPositionStageButton_Click 方法**
   - 改用风险金计算间距
   - 添加用户提示信息
   - 保持参数的智能继承

2. **InitializeStageDefaults 方法**
   - 默认阶梯基于风险金计算
   - 保盈止损阶梯同步优化
   - 动态生成描述信息

## 总结

这个功能将推仓阶梯设置从"一刀切"的固定模式升级为"个性化"的智能模式：

1. **智能化**：基于账户实际情况计算合理间距
2. **个性化**：不同规模账户有不同的阶梯设置
3. **风险一致性**：每个阶梯代表相同的风险级别
4. **用户友好**：提供详细的计算说明和透明度
5. **灵活性**：保持手动调整的能力

现在用户可以获得更符合自己风险承受能力和账户规模的推仓阶梯设置，实现真正的个性化风险管理。 