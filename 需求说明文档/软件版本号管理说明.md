# 软件版本号管理说明

## 版本号添加

按照用户要求，为软件添加了版本号显示功能，初始版本从 V0.31 开始。

## 版本号显示位置

### 1. 主窗口标题栏
在 `MainWindow.xaml` 中设置：
```xml
Title="币安期货交易管理器 V0.31"
```

用户在使用软件时，可以在窗口标题栏看到：
- 软件名称：币安期货交易管理器
- 版本号：V0.31

## 项目文件版本信息

### 2. 项目配置文件
在 `BinanceFuturesTrader.csproj` 中设置：
```xml
<AssemblyVersion>0.31.0.0</AssemblyVersion>
<FileVersion>0.31.0.0</FileVersion>
<Version>0.31</Version>
```

这样设置的好处：
- **AssemblyVersion**：程序集版本，用于.NET运行时识别
- **FileVersion**：文件版本，显示在文件属性中
- **Version**：产品版本，用于NuGet包等场景

## 版本号格式说明

### 当前版本格式
- **显示格式**：V0.31
- **完整格式**：0.31.0.0

### 版本号含义
```
主版本.次版本.修订版本.构建版本
0     .31    .0       .0

- 主版本 (0)：重大功能变更或架构重构
- 次版本 (31)：新功能添加或重要改进
- 修订版本 (0)：Bug修复或小改进
- 构建版本 (0)：构建序号
```

## 版本更新指南

### 何时更新版本号

1. **次版本更新 (0.31 → 0.32)**：
   - 添加新的重要功能
   - 重大界面改进
   - 重要性能优化
   - 新增交易策略或工具

2. **修订版本更新 (0.31.0 → 0.31.1)**：
   - Bug修复
   - 小的界面调整
   - 配置优化
   - 文档更新

3. **主版本更新 (0.31 → 1.0)**：
   - 软件功能完整稳定
   - 架构重大重构
   - 重大功能变更

### 版本更新步骤

当需要更新版本时，需要同时修改两个文件：

#### 1. 更新窗口标题
在 `MainWindow.xaml` 中：
```xml
Title="币安期货交易管理器 V0.32"  <!-- 更新版本号 -->
```

#### 2. 更新项目文件
在 `BinanceFuturesTrader.csproj` 中：
```xml
<AssemblyVersion>0.32.0.0</AssemblyVersion>  <!-- 更新程序集版本 -->
<FileVersion>0.32.0.0</FileVersion>          <!-- 更新文件版本 -->
<Version>0.32</Version>                      <!-- 更新产品版本 -->
```

## 版本历史记录

### V0.31 (当前版本)
- **发布日期**：2024年
- **主要功能**：
  - 币安期货交易管理
  - 持仓管理和风险控制
  - 自动止损和移动止损
  - 条件单和自动监控
  - 界面布局动态适配
  - 版本号显示功能

### 未来版本规划

#### V0.32 (计划)
- 新功能添加
- 性能优化
- 界面改进

#### V1.0 (目标)
- 功能完整稳定
- 全面测试通过
- 正式发布版本

## 版本号相关文件

### 需要同步更新的文件
1. `MainWindow.xaml` - 窗口标题显示
2. `BinanceFuturesTrader.csproj` - 项目版本信息

### 自动生成的版本相关文件
这些文件会在编译时自动生成，包含版本信息：
- `AssemblyInfo.cs`
- 可执行文件属性
- 安装包信息（如果制作安装包）

## 用户体验

### 版本号显示效果
用户可以通过以下方式查看版本信息：

1. **窗口标题栏**：
   ```
   币安期货交易管理器 V0.31
   ```

2. **文件属性**：
   - 右键点击exe文件 → 属性 → 详细信息
   - 可以看到完整的版本信息

3. **关于对话框**（未来可添加）：
   - 可以考虑在帮助菜单中添加"关于"对话框
   - 显示详细的版本和版权信息

## 技术实现细节

### XAML中的版本显示
```xml
<Window x:Class="BinanceFuturesTrader.MainWindow"
        Title="币安期货交易管理器 V0.31"
        Icon="Icons/app.ico">
```

### 项目文件中的版本配置
```xml
<PropertyGroup>
    <AssemblyVersion>0.31.0.0</AssemblyVersion>
    <FileVersion>0.31.0.0</FileVersion>
    <Version>0.31</Version>
    <AssemblyTitle>币安期货交易管理器</AssemblyTitle>
    <AssemblyProduct>Binance Futures Trader</AssemblyProduct>
</PropertyGroup>
```

## 最佳实践

### 版本号管理建议

1. **保持一致性**：确保所有地方的版本号同步更新
2. **有意义的版本号**：版本号应该反映软件的实际变化
3. **记录版本历史**：维护版本更新日志
4. **测试版本显示**：确保版本号正确显示在所有界面

### 发布流程建议

1. 完成功能开发
2. 更新版本号（两个文件）
3. 编译测试
4. 验证版本显示
5. 创建版本标签（Git）
6. 发布软件

## 总结

版本号管理系统现已建立：
- ✅ 窗口标题显示版本号 V0.31
- ✅ 项目文件包含完整版本信息
- ✅ 版本更新流程明确
- ✅ 为未来版本管理奠定基础

用户现在可以清楚地看到软件版本，便于跟踪功能更新和问题反馈。 