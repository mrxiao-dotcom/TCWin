# 查看监控按钮功能添加说明

## 功能概述

根据用户需求，新增了"查看监控"按钮，用于展示当前账户的自动监控配置信息和执行情况。该按钮与"自动盯盘"按钮并列放置在操作按钮区域的最右侧。

## 新增功能

### 1. 查看监控按钮

**位置**：操作按钮区域最右侧，紧邻"自动盯盘"按钮
**样式**：绿色背景（#2ECC71），白色文字，80像素宽度
**命令**：`ViewMonitorStatusCommand`

### 2. 功能特性

#### 监控状态展示
- **运行状态**：显示当前监控是否运行中
- **状态消息**：显示详细的状态信息
- **账户关联**：仅显示当前选择账户的信息

#### 配置信息展示
- **配置名称**：显示当前使用的配置名称
- **扫描间隔**：显示监控扫描的时间间隔
- **功能模块状态**：分别显示各功能模块的启用状态

#### 详细配置参数
1. **自动保本配置**
   - 启用状态
   - 触发盈利值

2. **自动推仓配置**
   - 启用状态
   - 启用阶梯数量
   - 每个阶梯的详细参数（触发盈利、风险金倍数、止损比例）

3. **自动保盈止损配置**
   - 启用状态
   - 启用阶梯数量
   - 每个阶梯的详细参数（触发盈利、保护金额）

#### 执行历史记录
- **最近执行记录**：显示最近5条执行记录
- **记录格式**：时间、合约、执行类型、成功状态
- **状态图标**：✅成功、❌失败

## 技术实现

### 1. 命令实现

**文件**：`ViewModels/MainViewModel.AutoMonitor.cs`
**方法**：`ViewMonitorStatusAsync()`

```csharp
[RelayCommand]
private async Task ViewMonitorStatusAsync()
{
    // 检查账户选择
    if (SelectedAccount == null)
    {
        MessageBox.Show("请先选择账户", "提示", MessageBoxButton.OK, MessageBoxImage.Information);
        return;
    }

    // 获取当前账户的配置信息
    var accountConfig = _accountAutoMonitorConfigs.TryGetValue(SelectedAccount.Name, out var config) ? config : null;
    
    // 构建状态信息并显示
    MessageBox.Show(statusMessage, "监控状态详情", MessageBoxButton.OK, MessageBoxImage.Information);
}
```

### 2. 界面布局调整

**文件**：`MainWindow.xaml`
**修改**：操作按钮区域布局

**调整前**：
```xml
<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
    <!-- 所有按钮居中排列 -->
</StackPanel>
```

**调整后**：
```xml
<DockPanel>
    <!-- 左侧按钮组 -->
    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
        <!-- 原有功能按钮 -->
    </StackPanel>
    
    <!-- 右侧监控按钮组 -->
    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="{Binding AutoMonitorButtonText}" Command="{Binding ToggleAutoMonitorCommand}"/>
        <Button Content="查看监控" Command="{Binding ViewMonitorStatusCommand}"/>
    </StackPanel>
    
    <!-- 中间填充空间 -->
    <Grid/>
</DockPanel>
```

### 3. 数据源获取

#### 配置信息来源
- **账户配置字典**：`_accountAutoMonitorConfigs`
- **当前运行状态**：`IsAutoMonitorRunning`、`AutoMonitorStatusMessage`

#### 执行历史来源
- **监控服务**：`_autoMonitorService.GetExecutionHistory()`
- **显示条数**：最近5条记录

## 显示内容示例

### 完整状态显示
```
=== 账户 测试账户 自动监控状态 ===

🔄 运行状态: 运行中
📊 状态消息: 自动盯盘运行中

⚙️ 配置名称: 默认配置
⏱️ 扫描间隔: 5秒

🛡️ 自动保本: 启用
   触发盈利: 10.00U

🚀 自动推仓: 启用
   启用阶梯: 4个
   阶梯1: 20.00U → 1.2倍风险金, 止损80.0%
   阶梯2: 50.00U → 1.5倍风险金, 止损70.0%
   阶梯3: 100.00U → 2.0倍风险金, 止损60.0%
   阶梯4: 200.00U → 2.5倍风险金, 止损50.0%

🛡️ 保盈止损: 启用
   启用阶梯: 3个
   阶梯1: 30.00U → 保护10.00U
   阶梯2: 80.00U → 保护30.00U
   阶梯3: 150.00U → 保护60.00U

📈 最近执行记录:
   ✅ 12-25 14:30:25 [BTCUSDT] 自动保本
   ✅ 12-25 14:35:10 [ETHUSDT] 推仓阶梯1
   ❌ 12-25 14:40:05 [ADAUSDT] 保盈止损阶梯1
```

### 无配置状态显示
```
=== 账户 新账户 自动监控状态 ===

🔄 运行状态: 未启动
📊 状态消息: 未启动

❌ 当前账户暂无监控配置

📈 最近执行记录:
   暂无执行记录
```

## 用户操作流程

1. **选择账户**：确保已选择要查看的账户
2. **点击按钮**：点击"查看监控"按钮
3. **查看信息**：在弹出的对话框中查看详细信息
4. **关闭对话框**：点击"确定"关闭信息窗口

## 使用场景

### 1. 配置验证
- 启动自动盯盘前，确认配置参数是否正确
- 检查各功能模块的启用状态

### 2. 运行监控
- 查看当前监控是否正常运行
- 了解最近的执行情况

### 3. 问题诊断
- 当自动盯盘功能异常时，查看配置和执行历史
- 确认触发条件和执行结果

### 4. 账户切换
- 切换账户后，查看新账户的监控配置
- 对比不同账户的配置差异

## 界面布局改进

### 布局优化
- **左右分离**：功能按钮在左侧，监控按钮在右侧
- **逻辑分组**：相关功能按钮聚集在一起
- **视觉平衡**：通过DockPanel实现更好的空间利用

### 按钮分组
1. **左侧功能组**：
   - 持仓操作：一键清仓、平仓、保本止损、保盈止损、分仓止盈
   - 系统功能：刷新
   - 委托管理：清理委托单、清除选定委托
   - 查询功能：查询订单历史

2. **右侧监控组**：
   - 自动盯盘：启动/停止监控
   - 查看监控：查看状态和配置

### 视觉设计
- **查看监控按钮**：绿色背景，与功能特性匹配
- **自动盯盘按钮**：保持原有的动态颜色（蓝色/红色）
- **分隔符**：使用竖线分隔不同功能组

## 总结

"查看监控"按钮的添加显著提升了自动盯盘系统的可用性：

1. **信息透明**：用户可以随时查看监控状态和配置
2. **操作便捷**：一键获取所有相关信息
3. **问题诊断**：通过执行历史快速定位问题
4. **界面优化**：按钮布局更加合理和美观

这个功能使得自动盯盘系统更加完善和用户友好，为用户提供了全面的监控管理体验。 