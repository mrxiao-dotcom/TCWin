# 自动盯盘默认设置修复说明

## 🚨 用户反馈问题

用户反映："我打开新的页面，点击自动盯盘，看到设置的默认还是跟原来一样，这是什么原因？"

## 🔍 问题根源分析

虽然我们修改了 `Models/AutoMonitorConfig.cs` 中的默认配置，但界面上显示的仍然是旧的默认值。这是因为：

### **1. 界面初始化使用了硬编码值**

在 `Views/AutoMonitorConfigDialog.xaml.cs` 的 `InitializeStageDefaults()` 方法中，使用了硬编码的旧默认值：

```csharp
// ❌ 问题代码（硬编码的旧默认值）
AddPositionStages.Add(new AddPositionStageViewModel { 
    Stage = 1, 
    TriggerProfitAmount = 20,        // 旧值：20U
    RiskCapitalMultiplier = 1.2m,    // 旧值：1.2倍
    StopLossPercentage = 80,         // 旧值：80%
    IsEnabled = true, 
    Description = "浮盈20U时推仓，风险金1.2倍，止损80%" 
});
```

### **2. 没有使用智能配置生成器**

界面创建时调用的是无参构造函数，没有传入账户信息：

```csharp
// ❌ 问题代码（没有账户信息）
var configDialog = new AutoMonitorConfigDialog();
```

## 🛠️ 修复方案

### **1. 添加智能构造函数**

为 `AutoMonitorConfigDialog` 添加了带账户信息的构造函数：

```csharp
/// <summary>
/// 使用账户信息的构造函数（推荐使用）
/// </summary>
/// <param name="accountEquity">账户权益</param>
/// <param name="riskCapitalTimes">风险金倍数</param>
public AutoMonitorConfigDialog(decimal accountEquity, int riskCapitalTimes) : this()
{
    _accountEquity = accountEquity > 0 ? accountEquity : 1000m;
    _riskCapitalTimes = riskCapitalTimes > 0 ? riskCapitalTimes : 10;
    
    // 重新初始化为智能默认配置
    InitializeSmartDefaults();
}
```

### **2. 实现智能默认初始化**

添加了 `InitializeSmartDefaults()` 方法：

```csharp
private void InitializeSmartDefaults()
{
    // 使用智能配置生成器
    var smartConfig = AutoMonitorConfig.CreateSmartDefault(_accountEquity, _riskCapitalTimes);
    
    // 基础设置
    ConfigNameTextBox.Text = smartConfig.Name;
    
    // 自动保本设置
    BreakEvenTriggerTextBox.Text = smartConfig.BreakEvenConfig.TriggerProfitAmount.ToString("F0");
    
    // 初始化智能阶梯配置
    InitializeSmartStageDefaults(smartConfig);
}
```

### **3. 更新调用方式**

修改 `MainViewModel.AutoMonitor.cs` 中的调用：

```csharp
// ✅ 修复后的代码（传入账户信息）
var accountEquity = AccountInfo?.TotalEquity ?? 1000m;
var riskCapitalTimes = SelectedAccount.RiskCapitalTimes;
var configDialog = new AutoMonitorConfigDialog(accountEquity, riskCapitalTimes);
```

### **4. 更新旧版兼容**

即使使用无参构造函数，也会显示新的默认值：

```csharp
// 推仓阶梯默认配置（使用新的智能默认值）
AddPositionStages.Add(new AddPositionStageViewModel { 
    Stage = 1, 
    TriggerProfitAmount = 100,    // 新值：100U
    RiskCapitalMultiplier = 1.0m, // 新值：1.0倍
    StopLossPercentage = 10,      // 新值：10%
    IsEnabled = true, 
    Description = "浮盈100U时推仓，风险金1.0倍，止损10%" 
});
```

## ✅ 修复效果

### **修复前的界面**：
```
推仓阶梯：
- 第1档：20U → 1.2倍风险金，80%止损
- 第2档：50U → 1.5倍风险金，70%止损
- 第3档：100U → 2.0倍风险金，60%止损
- 第4档：200U → 2.5倍风险金，50%止损

保盈阶梯：
- 第1档：30U → 保护10U
- 第2档：80U → 保护30U
- 第3档：150U → 保护60U
```

### **修复后的界面（以1000U账户为例）**：
```
推仓阶梯：
- 第1档：100U → 1.0倍风险金，10%止损
- 第2档：200U → 1.0倍风险金，10%止损
- 第3档：300U → 1.0倍风险金，10%止损
- 第4档：400U → 1.0倍风险金，10%止损

保盈阶梯：
- 第1档：1000U → 保护800U
- 第2档：2000U → 保护1600U
- 第3档：3000U → 保护2400U
```

## 🎯 智能化特性

### **1. 账户适配**
- **小账户（500U）**：推仓50U/100U/150U/200U
- **中账户（2000U）**：推仓200U/400U/600U/800U
- **大账户（10000U）**：推仓1000U/2000U/3000U/4000U

### **2. 配置名称智能化**
- 显示为"智能配置（权益1000U）"
- 一看就知道是基于哪个账户权益生成的

### **3. 比例化保护**
- 保盈触发：风险金的10/20/30倍
- 保护金额：触发金额的80%
- 既保护利润，又留有回撤空间

## 🔄 向后兼容

- **新用户**：自动使用智能默认配置
- **老用户**：保存的配置不受影响
- **无账户信息**：回退到通用默认配置

## 📁 修改文件清单

| 文件路径 | 修改内容 | 状态 |
|---------|---------|------|
| `Models/AutoMonitorConfig.cs` | 添加智能配置生成器 | ✅ 完成 |
| `Views/AutoMonitorConfigDialog.xaml.cs` | 添加智能构造函数和初始化 | ✅ 完成 |
| `ViewModels/MainViewModel.AutoMonitor.cs` | 更新调用方式传入账户信息 | ✅ 完成 |

现在用户打开自动盯盘配置页面时，会看到基于其账户权益和风险设置动态生成的智能默认配置，而不是固定的旧默认值。 