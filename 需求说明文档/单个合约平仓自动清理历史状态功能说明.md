# 单个合约平仓自动清理历史状态功能说明

## 功能背景

用户反馈了一个重要的使用场景：
> "当一个合约从持仓中被平仓了，对应的自动盯盘历史状态也要清理"

这是对之前实现的一键清仓自动清理功能的重要补充，确保单个合约平仓时也能自动清理其历史状态。

## 问题分析

### 现有清理机制的局限性

之前实现的清理机制有以下限制：

1. **一键清仓清理**：只在全部持仓清空后才清理所有历史状态
2. **自动监控清理**：只在自动监控扫描时清理已平仓合约（有延迟）
3. **手动清理**：需要用户主动操作

### 缺失的清理场景

- **单个持仓平仓**：用户选择单个持仓进行平仓
- **批量持仓平仓**：用户选择多个持仓进行批量平仓
- **部分清仓**：不是全部清仓，但部分合约已经平掉

### 具体影响

```
用户操作场景：
1. 持有多个合约：BTCUSDT LONG, ETHUSDT SHORT, ADAUSDT LONG
2. 自动盯盘为每个合约都设置了推仓/保本/止盈
3. 用户单独平掉 BTCUSDT LONG ✅
4. BTCUSDT LONG 的历史状态仍然存在 ❌
5. 重新开仓 BTCUSDT LONG 时无法触发阶梯 ❌
```

## 解决方案

### 1. 新增合约级别的历史状态清理

我在 `ViewModels/MainViewModel.PositionManagement.cs` 中添加了 `CleanupAutoMonitorHistoryForPositionAsync` 方法，专门处理单个合约的历史状态清理。

#### 1.1 方法实现

```csharp
/// <summary>
/// 清理指定持仓的自动盯盘历史状态
/// </summary>
private async Task CleanupAutoMonitorHistoryForPositionAsync(string symbol, string positionSide, string reason)
{
    try
    {
        _logger.LogInformation($"🧹 开始清理合约 {symbol}_{positionSide} 的自动盯盘历史状态 (原因: {reason})");
        
        // 清理持久化存储中的状态
        var persistenceService = new AutoMonitorPersistenceService();
        persistenceService.CleanupContractHistory(symbol, positionSide, reason);
        
        // 如果自动盯盘服务正在运行，也清理其内存状态
        if (_autoMonitorService != null)
        {
            var profiles = _autoMonitorService.GetPositionProfiles();
            var contractKey = $"{symbol}_{positionSide}";
            
            if (profiles.ContainsKey(contractKey))
            {
                var triggerCount = profiles[contractKey].TriggerRecords.Count;
                _logger.LogInformation($"🔄 发现自动盯盘内存状态需要清理: {contractKey} - {triggerCount}个触发记录");
                
                // 通过重启自动盯盘服务来清理内存状态
                if (_isAutoMonitorRunning && _currentAutoMonitorConfig != null)
                {
                    _autoMonitorService.StopMonitoring();
                    await Task.Delay(500);
                    await _autoMonitorService.StartMonitoringAsync(_currentAutoMonitorConfig);
                    _logger.LogInformation("🔄 已重启自动盯盘服务，内存状态已同步");
                }
            }
        }
        
        _logger.LogInformation($"✅ 合约 {symbol}_{positionSide} 的自动盯盘历史状态清理完成");
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, $"❌ 清理合约 {symbol}_{positionSide} 的自动盯盘历史状态时发生异常");
        // 不影响主流程，继续执行
    }
}
```

#### 1.2 清理范围

- **持久化存储清理**：删除 `position_profiles.json` 中对应合约的档案
- **执行历史清理**：删除 `execution_history.json` 中对应合约的执行记录
- **内存状态清理**：清理自动盯盘服务内存中的对应状态
- **服务同步**：重启自动盯盘服务确保内存状态同步

### 2. 集成到所有平仓功能

#### 2.1 单个持仓平仓

在 `ClosePositionAsync` 方法中添加清理：

```csharp
if (success)
{
    StatusMessage = $"持仓 {SelectedPosition.Symbol} 平仓成功";
    _logger.LogInformation($"持仓平仓成功: {SelectedPosition.Symbol}");
    
    // 🔧 新增：清理对应合约的自动盯盘历史状态
    await CleanupAutoMonitorHistoryForPositionAsync(SelectedPosition.Symbol, SelectedPosition.PositionSideString, "单持仓平仓");
    
    // 刷新数据
    await RefreshDataAsync();
}
```

#### 2.2 批量持仓平仓

在 `CloseSelectedPositionsAsync` 方法中添加清理：

```csharp
if (success)
{
    successCount++;
    _logger.LogInformation($"持仓平仓成功: {position.Symbol} {position.PositionSideString}");
    
    // 🔧 新增：清理对应合约的自动盯盘历史状态
    await CleanupAutoMonitorHistoryForPositionAsync(position.Symbol, position.PositionSideString, "批量平仓");
}
```

#### 2.3 一键清仓中的单合约清理

在 `ClearAllPositionsAndOrdersAsync` 方法的第二步中添加：

```csharp
if (closeResult)
{
    successOperations++;
    _logger.LogInformation($"✅ 平仓成功: {position.Symbol} {position.PositionSideString} {quantity:F6}");
    
    // 🔧 新增：立即清理该合约的自动盯盘历史状态
    await CleanupAutoMonitorHistoryForPositionAsync(position.Symbol, position.PositionSideString, "一键清仓-单合约");
}
```

## 功能特性

### 1. 即时清理

- ✅ **实时触发**：合约平仓成功立即清理，无延迟
- ✅ **精确清理**：只清理对应合约的状态，不影响其他合约
- ✅ **异常隔离**：清理失败不影响平仓主流程

### 2. 全面覆盖

- ✅ **单个平仓**：选择单个持仓平仓时自动清理
- ✅ **批量平仓**：批量选择多个持仓平仓时逐个清理
- ✅ **一键清仓**：既有单合约清理，又有全局清理

### 3. 双重保障

- ✅ **持久化清理**：清理存储文件中的状态
- ✅ **内存清理**：清理运行时的内存状态
- ✅ **服务同步**：重启自动盯盘确保状态一致

### 4. 详细记录

- ✅ **清理原因**：记录清理的具体原因（单持仓平仓、批量平仓等）
- ✅ **清理日志**：详细记录清理过程和结果
- ✅ **审计追踪**：所有清理操作都可追踪

## 清理时机对比

| 清理方式 | 触发时机 | 清理范围 | 适用场景 |
|---------|---------|----------|----------|
| **自动监控清理** | 扫描时检测到平仓 | 已平仓合约 | 后台自动清理 |
| **一键清仓清理** | 全部持仓清空后 | 所有合约 | 完全清仓 |
| **单合约清理** 🆕 | 合约平仓成功时 | 对应合约 | 部分平仓 |
| **手动清理** | 用户主动操作 | 指定合约/全部 | 特殊情况 |

## 使用效果

### 清理前后对比

**清理前（单个合约平仓）**：
```
1. 平仓 BTCUSDT LONG ✅
2. 持仓表中 BTCUSDT LONG 消失 ✅
3. 自动盯盘历史状态残留 ❌
   - position_profiles.json 仍包含 BTCUSDT_LONG 档案
   - execution_history.json 仍包含执行记录
4. 重新开仓 BTCUSDT LONG 时无法触发阶梯 ❌
```

**清理后（单个合约平仓）**：
```
1. 平仓 BTCUSDT LONG ✅
2. 持仓表中 BTCUSDT LONG 消失 ✅
3. 自动盯盘历史状态同步清理 ✅
   - position_profiles.json 中 BTCUSDT_LONG 档案已删除
   - execution_history.json 中对应记录已删除
4. 重新开仓 BTCUSDT LONG 时正常触发所有阶梯 ✅
```

### 日志记录示例

```
持仓平仓成功: BTCUSDT LONG
🧹 开始清理合约 BTCUSDT_LONG 的自动盯盘历史状态 (原因: 单持仓平仓)
🗑️ 清理合约档案: BTCUSDT_LONG - 清理3个触发记录 (原因: 单持仓平仓)
🗑️ 清理执行历史: BTCUSDT_LONG - 移除5条记录 (原因: 单持仓平仓)
🔄 发现自动盯盘内存状态需要清理: BTCUSDT_LONG - 3个触发记录
🔄 已重启自动盯盘服务，内存状态已同步
✅ 合约 BTCUSDT_LONG 的自动盯盘历史状态清理完成
```

## 技术要点

### 1. 异常处理

- 清理操作被完全包装在 try-catch 中
- 清理失败不会影响平仓的主要流程
- 异常详细记录到日志中

### 2. 性能考虑

- 清理操作异步执行，不阻塞UI
- 内存清理通过服务重启实现，确保彻底
- 只重启自动盯盘服务，不影响其他功能

### 3. 并发安全

- 使用持久化服务的线程安全方法
- 清理操作有适当的延迟避免竞态条件
- 服务重启过程有序进行

## 总结

这个功能完美补充了自动盯盘历史状态清理机制：

- ✅ **全场景覆盖**：单个平仓、批量平仓、一键清仓都有对应清理
- ✅ **即时响应**：合约平仓成功立即清理，无需等待
- ✅ **精确清理**：只清理对应合约，不影响其他合约状态
- ✅ **双重保障**：既清理持久化存储，又清理内存状态
- ✅ **异常安全**：清理失败不影响平仓主流程

现在用户无论使用哪种方式平仓合约，对应的自动盯盘历史状态都会被自动清理，确保重新开仓时能正常触发所有配置的阶梯功能。 