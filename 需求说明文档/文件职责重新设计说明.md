# 文件职责重新设计说明

## 🚨 **设计纠正**

根据用户反馈，我之前混淆了两个文件的职责。现在重新明确设计：

## 📁 **正确的文件职责**

### **1. 基础配置模板文件：`AutoMonitorConfigs.json`**

**📍 职责**：
- 存储用户自定义的配置模板
- 只能在模板编辑页面中修改
- 纯配置数据，不包含执行状态

**📋 数据结构**：
```json
{
  "稳健": {
    "name": "稳健",
    "isEnabled": true,
    "scanIntervalSeconds": 5,
    "cooldownSeconds": 5,
    "breakEvenConfig": {
      "isEnabled": true,
      "triggerProfitAmount": 10.0
    },
    "addPositionConfig": {
      "isEnabled": true,
      "tiers": [
        {
          "tierIndex": 1,
          "isEnabled": true,
          "triggerProfitAmount": 100.0,
          "riskMultiplier": 1.0,
          "stopLossRatio": 0.10,
          "profitProtectionAmount": 0,
          "exitTargetPnl": 0
        }
        // ... 其他阶梯
      ]
    },
    "profitProtectionConfig": {
      "isEnabled": true,
      "tiers": [
        {
          "tierIndex": 1,
          "isEnabled": true,
          "triggerProfitAmount": 1000.0,
          "protectionAmount": 800.0
        }
        // ... 其他阶梯
      ]
    }
  },
  "狂飙": {
    "name": "狂飙",
    // ... 狂飙配置
  }
}
```

### **2. 统一状态文件：`contract_monitoring_states.json`**

**📍 职责**：
- 每个持仓的完整配置数据 + 执行状态
- 所有UI、持久化、手工、自动扫描的关键中间文件
- 当切换配置时，从基础模板同步配置数据

**📋 数据结构**：
```json
{
  "BTCUSDT_LONG": {
    "archiveId": "uuid-here",
    "symbol": "BTCUSDT",
    "positionSide": "LONG",
    "baseConfigName": "稳健",  // 引用基础配置名称
    
    // 持仓信息
    "currentQuantity": 0.001,
    "currentEntryPrice": 45000.0,
    "currentMarkPrice": 46000.0,
    "currentUnrealizedPnl": 150.0,
    "isActive": true,
    "createTime": "2025-01-19T12:00:00",
    "lastUpdateTime": "2025-01-19T12:00:00",
    
    // 完整的配置数据（从基础模板复制）+ 执行状态
    "name": "稳健_BTCUSDT",
    "isEnabled": true,
    "scanIntervalSeconds": 5,
    "cooldownSeconds": 5,
    
    "breakEvenConfig": {
      // 从基础配置复制的设置
      "isEnabled": true,
      "triggerProfitAmount": 10.0,
      
      // 执行状态字段
      "isExecuted": false,
      "executionTime": null,
      "executionPnl": 0,
      "executionStatus": "未触发",
      "executionResult": ""
    },
    
    "addPositionConfig": {
      "isEnabled": true,
      "tiers": [
        {
          // 从基础配置复制的设置
          "tierIndex": 1,
          "isEnabled": true,
          "triggerProfitAmount": 100.0,
          "riskMultiplier": 1.0,
          "stopLossRatio": 0.10,
          "profitProtectionAmount": 0,
          "exitTargetPnl": 0,
          
          // 执行状态字段
          "isExecuted": false,
          "executionTime": null,
          "executionPnl": 0,
          "executionStatus": "未触发",
          "executionResult": "",
          "addPositionQuantity": 0,
          "stopLossPrice": 0
        }
        // ... 其他阶梯
      ]
    },
    
    "profitProtectionConfig": {
      "isEnabled": true,
      "tiers": [
        {
          // 从基础配置复制的设置 + 执行状态
          "tierIndex": 1,
          "isEnabled": true,
          "triggerProfitAmount": 1000.0,
          "protectionAmount": 800.0,
          
          // 执行状态字段
          "isExecuted": false,
          "executionTime": null,
          "executionPnl": 0,
          "executionStatus": "未触发",
          "executionResult": "",
          "stopLossPrice": 0
        }
        // ... 其他阶梯
      ]
    }
  }
}
```

## 🔄 **工作流程**

### **1. 配置模板管理**
```
用户 → 模板编辑器 → AutoMonitorConfigs.json
```

### **2. 配置应用到合约**
```
用户选择配置 → 系统从 AutoMonitorConfigs.json 读取模板 
→ 更新 contract_monitoring_states.json 中对应合约的配置数据
```

### **3. 执行状态跟踪**
```
执行引擎 ↔ contract_monitoring_states.json ↔ UI显示
```

### **4. 配置切换**
```
用户切换配置 → 系统重新从 AutoMonitorConfigs.json 读取新模板
→ 更新 contract_monitoring_states.json（保留执行状态）
```

## 📍 **关键设计原则**

1. **单一数据源**：`AutoMonitorConfigs.json` 是配置模板的唯一来源
2. **状态分离**：配置数据 与 执行状态 分离管理
3. **引用关系**：统一状态文件通过 `baseConfigName` 引用基础配置
4. **状态保留**：切换配置时保留现有的执行状态
5. **完整复制**：统一状态文件包含完整的配置数据副本，避免频繁读取基础配置

## 🎯 **修正后的优势**

- ✅ **职责清晰**：基础配置 vs 运行状态 分离
- ✅ **数据一致**：配置切换时自动同步
- ✅ **状态完整**：包含完整配置 + 执行状态
- ✅ **性能优化**：避免频繁读取基础配置文件
- ✅ **扩展性强**：支持灵活的配置管理 