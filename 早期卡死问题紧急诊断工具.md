# 早期卡死问题紧急诊断工具

## 🚨 问题描述
点击"启动盯盘"按钮后，按钮显示"正在启动"就立即卡死，连日志都看不到。

## 🔍 紧急诊断步骤

### 第一步：确认诊断日志级别
1. 确保日志级别设置为 **DEBUG** 或 **TRACE**
2. 确保能看到 **CRITICAL** 级别的日志
3. 重新启动程序，确保日志配置生效

### 第二步：查找关键诊断标记
点击"启动盯盘"后，应该依次出现以下日志：

```
[CRITICAL] 🔍 [CLICK-01] 按钮点击事件触发
[CRITICAL] 🔍 [CLICK-02] 检查服务状态: False
[CRITICAL] 🔍 [CLICK-04] 执行启动盯盘流程
[CRITICAL] 🔍 [ENTRY-01] HandleStartMonitoring 方法开始执行
[CRITICAL] 🔍 [ENTRY-02] 准备更新按钮状态
[CRITICAL] 🔍 [UI-UPDATE-01] 开始更新按钮状态: 正在启动...
[CRITICAL] 🔍 [UI-UPDATE-02] 在UI线程，直接更新
[CRITICAL] 🔍 [UI-UPDATE-03] UI线程直接更新完成
[CRITICAL] 🔍 [ENTRY-03] 按钮状态更新完成
[CRITICAL] 🔍 [ENTRY-04] 进入try块，准备创建超时控制
[CRITICAL] 🔍 [ENTRY-05] 超时控制创建完成，准备启动Task.Run
[CRITICAL] 🔍 [ENTRY-06] 即将执行Task.Run
[CRITICAL] 🔍 [TASK-01] Task.Run内部开始执行
[CRITICAL] 🔍 [PERFORM-01] PerformStartMonitoringAsync 开始执行
```

### 第三步：根据最后一个日志定位问题

| 最后出现的日志 | 问题位置 | 可能原因 | 解决方案 |
|---------------|---------|---------|---------|
| 没有任何日志 | 按钮事件未触发 | UI线程已死锁 | 重启程序，检查其他UI操作 |
| [CLICK-01] | 点击事件内部卡死 | 服务状态检查阻塞 | 检查_autoMonitorService初始化 |
| [CLICK-02] | HandleStartMonitoring调用前 | await调用问题 | 检查async/await使用 |
| [ENTRY-01] | 按钮状态更新前 | UpdateToggleButtonState问题 | UI线程访问问题 |
| [UI-UPDATE-01] | Dispatcher调用 | UI线程死锁 | 检查UI线程状态 |
| [ENTRY-04] | Task.Run创建前 | CancellationTokenSource问题 | 内存或资源问题 |
| [ENTRY-06] | Task.Run执行 | Task调度器问题 | 线程池耗尽 |

## 🔧 应急修复方案

### 方案1：检查日志输出
如果完全看不到日志，问题可能在：
1. **日志配置问题**：确认日志级别和输出目标
2. **UI线程死锁**：整个程序无响应
3. **进程挂起**：检查任务管理器中的进程状态

### 方案2：最小化测试
添加最简单的测试代码到按钮点击事件开头：
```csharp
private async void ToggleMonitorButton_Click(object sender, RoutedEventArgs e)
{
    MessageBox.Show("按钮被点击了"); // 添加这行测试
    // ... 原有代码
}
```

### 方案3：同步启动测试
临时将启动改为同步执行（仅用于诊断）：
```csharp
private void ToggleMonitorButton_Click(object sender, RoutedEventArgs e)
{
    _logger.LogCritical("🔍 [TEST] 同步测试开始");
    UpdateToggleButtonState(false, "测试中...", Colors.Yellow, false);
    _logger.LogCritical("🔍 [TEST] 按钮更新完成");
    
    // 暂时注释掉async调用
    // await HandleStartMonitoring();
}
```

## 🚑 紧急处理流程

### 情况1：完全无响应
- **现象**：点击按钮后程序完全卡死，无任何日志
- **处理**：
  1. 强制结束程序进程
  2. 检查系统资源使用情况
  3. 以管理员权限重新运行
  4. 尝试最小化测试

### 情况2：有部分日志
- **现象**：能看到部分[CLICK-XX]或[ENTRY-XX]日志，然后卡死
- **处理**：
  1. 记录最后一个日志标记
  2. 对照上表找到具体问题位置
  3. 针对性检查对应组件

### 情况3：日志正常但UI卡死
- **现象**：日志持续输出，但界面无响应
- **处理**：
  1. 这说明后台处理正常，问题在UI更新
  2. 检查Dispatcher调用
  3. 检查UI属性绑定

## 🔬 深度诊断工具

### 工具1：进程监控
```cmd
# 检查进程CPU和内存使用
tasklist /fi "imagename eq YourApp.exe" /fo table /v

# 检查线程状态
wmic process where name="YourApp.exe" get ProcessId,ThreadCount,PageFileUsage
```

### 工具2：事件日志检查
1. 打开"事件查看器"
2. 查看"Windows日志" > "应用程序"
3. 搜索程序相关的错误或警告

### 工具3：Visual Studio调试
如果有开发环境：
1. 附加到进程进行调试
2. 查看调用堆栈
3. 检查线程状态

## 📋 信息收集清单

当问题发生时，请收集以下信息：

- [ ] 最后出现的日志标记：`[    ]`
- [ ] 程序是否完全无响应：`是/否`
- [ ] 按钮状态是否更新：`是/否`
- [ ] 其他UI操作是否正常：`是/否`
- [ ] 系统CPU使用率：`____%`
- [ ] 系统内存使用率：`____%`
- [ ] 是否有错误弹窗：`是/否`
- [ ] 程序运行时长：`___分钟`
- [ ] 是否能正常关闭程序：`是/否`

## 📞 联系支持

如果问题仍然无法解决，请提供：
1. 完整的日志文件（即使很短）
2. 上述信息收集清单
3. 系统环境信息（Windows版本、.NET版本等）
4. 详细的操作步骤

---

**创建时间**: 2024年1月9日  
**适用版本**: v2024.1.9及以后  
**紧急程度**: 🔴 最高优先级 