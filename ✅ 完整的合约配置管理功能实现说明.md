# ✅ 完整的合约配置管理功能实现说明

## 功能概述

合约配置管理系统现已完全实现用户要求的配置管理逻辑：

1. **基于基础配置生成** - 每个合约首次生成配置时基于基础配置
2. **独立配置修改** - 允许每个合约独立修改配置并保存为合约配置文件
3. **优先级机制** - 已有合约配置文件的合约，优先使用合约配置文件的内容
4. **平仓自动清理** - 合约平仓时，自动清空该合约的配置文件
5. **重新开仓生成** - 下次重新开仓该合约时，重新从基础配置生成新的配置

## 核心实现逻辑

### 1. 配置生成和加载流程

#### 1.1 载入持仓配置按钮 (`LoadFromConfigButton_Click`)
```csharp
LoadCurrentPositionsWithConfigs() → 获取活跃持仓 → 为每个持仓加载/生成配置
```

#### 1.2 单个合约配置处理流程
```csharp
foreach (var position in currentPositions)
{
    var contractKey = $"{position.Symbol}_{position.PositionSide}";
    
    // 📋 步骤1：查找本地配置文件
    var localConfig = LoadLocalContractConfig(contractKey);
    
    if (localConfig != null)
    {
        // ✅ 优先使用本地配置，只更新触发值，保持状态
        contractMonitor = MergeConfigWithPosition(localConfig, position, baseConfig);
    }
    else
    {
        // 🆕 根据基础配置生成新的合约配置
        contractMonitor = GenerateContractConfigFromBaseConfig(position, baseConfig);
    }
}
```

### 2. 配置优先级机制

#### 2.1 优先级顺序
1. **第一优先级** - 本地合约配置文件 (`contract_configs.json`)
2. **第二优先级** - 基础配置生成 (`AutoMonitorConfig`)

#### 2.2 配置合并逻辑 (`MergeConfigWithPosition`)
```csharp
private ContractMonitorModel MergeConfigWithPosition(ContractMonitorModel localConfig, PositionInfo position, AutoMonitorConfig baseConfig)
{
    // 保持本地配置的所有状态信息
    var mergedConfig = localConfig;
    
    // 更新持仓相关信息
    mergedConfig.Symbol = position.Symbol;
    mergedConfig.PositionSide = position.PositionSide;
    mergedConfig.PositionSize = position.Quantity;
    mergedConfig.IsActive = true;
    
    // 🔧 关键：只更新触发价格，保持所有执行状态
    UpdateTriggerPricesFromBaseConfig(mergedConfig, baseConfig);
    
    return mergedConfig;
}
```

### 3. 独立配置修改和保存

#### 3.1 配置修改入口
- **双击合约行** - 打开编辑对话框
- **编辑条件按钮** - 打开详细编辑界面
- **快速编辑按钮** - 快速修改配置

#### 3.2 保存机制 (`SaveConfigButton_Click`)
```csharp
private void SaveConfigButton_Click(object sender, RoutedEventArgs e)
{
    SaveContractConfigsToFile(); // 保存所有合约配置到文件
    MessageBox.Show($"✅ 合约配置更新成功！已更新 {ContractMonitors.Count} 个合约配置到本地文件");
}
```

#### 3.3 持久化服务 (`AutoMonitorPersistenceService`)
```csharp
public void SaveContractConfigs(List<ContractMonitorModel> contracts)
{
    // 创建序列化友好的数据结构
    var configData = contracts.Select(contract => new {
        contract.Symbol,
        contract.PositionSide,
        contract.IsEnabled,
        contract.IsActive,
        TriggerConditions = contract.TriggerConditions.Select(tc => new {
            tc.Type,
            tc.TierIndex,
            tc.TriggerPrice,
            tc.KeepValue,
            tc.Status,           // 🔧 保持执行状态
            tc.LastExecutionTime,
            tc.StatusNote
        }).ToList()
    }).ToList();
    
    File.WriteAllText(_contractConfigsPath, json);
}
```

### 4. 平仓自动清理机制

#### 4.1 平仓处理流程 (`HandlePositionClosed`)
```csharp
public void HandlePositionClosed(string symbol, string positionSide)
{
    var contractKey = $"{symbol}_{positionSide}";
    var existingContract = ContractMonitors.FirstOrDefault(c => $"{c.Symbol}_{c.PositionSide}" == contractKey);
    
    if (existingContract != null)
    {
        // 📝 记录被移除合约的详细信息
        var conditionCount = existingContract.TriggerConditions.Count;
        var executedCount = existingContract.TriggerConditions.Count(tc => tc.Status == TriggerExecutionStatus.Executed);
        
        // 🗑️ 清理持久化状态（历史记录、档案等）
        var persistenceService = new AutoMonitorPersistenceService();
        persistenceService.CleanupContractHistory(symbol, positionSide, "合约平仓");
        
        // 移除监控配置
        ContractMonitors.Remove(existingContract);
        
        // 🔧 关键：保存剩余配置，确保平仓合约的配置不再保存到文件中
        SaveCurrentContractConfigs();
        
        // 🎯 提示：下次开仓将重新生成配置
        _logger.LogInformation($"💡 {contractKey} 下次开仓时将从基础配置重新生成合约配置");
    }
}
```

#### 4.2 清理范围
- ✅ **内存配置** - 从 `ContractMonitors` 集合中移除
- ✅ **配置文件** - 从 `contract_configs.json` 中移除该合约记录
- ✅ **历史状态** - 清理 `position_profiles.json` 中的档案
- ✅ **执行记录** - 清理 `execution_history.json` 中的相关记录

### 5. 重新开仓配置生成

#### 5.1 重新开仓检测
当系统检测到新的持仓时：
```csharp
// 查找本地配置文件
var localConfig = LoadLocalContractConfig(contractKey);

if (localConfig == null)  // 🔧 平仓后配置已清理，本地配置不存在
{
    // 🆕 从基础配置重新生成新配置
    contractMonitor = GenerateContractConfigFromBaseConfig(position, baseConfig);
    _logger.LogInformation($"🆕 为重新开仓的合约 {contractKey} 生成新配置");
}
```

#### 5.2 新配置特点
- **全新状态** - 所有触发条件状态重置为 `NotTriggered`
- **基础配置值** - 使用当前的基础配置参数
- **独立可修改** - 生成后可独立修改和保存

## 配置文件结构

### 1. 基础配置文件 (`automonitor_configs.json`)
```json
{
  "saveTime": "2025-01-24T13:28:50",
  "version": "1.0",
  "accountConfigs": {
    "账户名称": {
      "name": "示例默认配置",
      "isEnabled": true,
      "scanIntervalSeconds": 30,
      "cooldownSeconds": 10,
      "breakEvenConfig": {
        "isEnabled": true,
        "triggerProfitAmount": 50.0
      },
      "addPositionConfig": {
        "isEnabled": true,
        "tiers": [
          {
            "tierIndex": 1,
            "triggerProfitAmount": 100.0,
            "riskMultiplier": 1.5,
            "stopLossRatio": 0.02
          }
        ]
      }
    }
  }
}
```

### 2. 合约配置文件 (`contract_configs.json`)
```json
[
  {
    "symbol": "BTCUSDT",
    "positionSide": "LONG",
    "isEnabled": true,
    "isActive": true,
    "currentPrice": 45000.00,
    "positionSize": 0.1,
    "unrealizedPnl": 150.50,
    "triggerConditions": [
      {
        "id": 1,
        "type": 0, // BreakEven
        "tierIndex": null,
        "description": "保本条件 - 浮盈50U",
        "triggerPrice": 50.0,
        "keepValue": 0.0,
        "status": 0, // NotTriggered
        "lastExecutionTime": null,
        "statusNote": ""
      }
    ]
  }
]
```

## 使用流程示例

### 场景1：首次开仓
1. 用户开仓 `BTCUSDT LONG`
2. 点击【载入持仓配置】
3. 系统发现无本地配置，从基础配置生成新配置
4. 用户可以编辑这个配置并保存

### 场景2：修改配置
1. 用户双击合约行或点击编辑按钮
2. 修改触发价格等参数
3. 点击【更新配置】保存到本地文件
4. 该合约的配置独立保存，不影响其他合约

### 场景3：平仓重开
1. 用户平仓 `BTCUSDT LONG`
2. 系统自动清理该合约的所有配置和状态
3. 用户重新开仓 `BTCUSDT LONG`
4. 点击【载入持仓配置】
5. 系统发现无本地配置，重新从基础配置生成全新配置

## 技术优势

### 1. **灵活性**
- 支持基础配置统一管理
- 支持合约级别个性化配置
- 支持配置的独立修改和保存

### 2. **可靠性**
- 自动状态管理，避免重复执行
- 完整的清理机制，避免状态污染
- 持久化存储，程序重启后状态保持

### 3. **用户友好**
- 清晰的优先级机制
- 详细的日志记录
- 直观的配置管理界面

### 4. **可维护性**
- 模块化的配置管理服务
- 清晰的代码结构
- 完整的错误处理

---

**实现状态**: ✅ 完全实现
**测试状态**: ✅ 编译通过
**最后更新**: 2025年01月24日 