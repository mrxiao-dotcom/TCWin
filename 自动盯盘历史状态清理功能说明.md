# 自动盯盘历史状态清理功能说明

## 功能背景

用户反馈了一个重要问题：
> "自动盯盘的记录里，可以保存历史状态，但也要对历史合约的状态进行清理，因为有些合约平仓后，过去的保本、加仓、止盈的历史状态要随之清理，不然影响新开仓的操作"

## 问题分析

### 核心问题
当合约平仓后重新开仓时，历史的触发记录会阻止相同阶梯的重新执行，导致：
- **推仓阶梯无法重新触发**：之前执行过的阶梯1-4会被跳过
- **保本止损无法重新设置**：历史记录显示已执行过保本
- **保盈止损无法重新配置**：之前的保盈设置会影响新持仓

### 具体场景
```
第一次开仓 BTCUSDT LONG:
12:00:00 - 浮盈50U，触发推仓阶梯1 ✅
12:00:05 - 浮盈100U，触发推仓阶梯2 ✅
12:00:10 - 浮盈150U，触发保本止损 ✅

平仓后重新开仓 BTCUSDT LONG:
12:30:00 - 浮盈50U，推仓阶梯1被跳过 ❌ (历史记录显示已执行)
12:30:05 - 浮盈100U，推仓阶梯2被跳过 ❌ (历史记录显示已执行)
12:30:10 - 浮盈150U，保本止损被跳过 ❌ (历史记录显示已执行)
```

## 解决方案

### 1. 自动清理机制

#### 1.1 增强的 `CleanupClosedPositions` 方法

```csharp
// 🔧 增强：记录清理的档案信息并检测重新开仓情况
private void CleanupClosedPositions(List<PositionInfo> activePositions)
{
    // 清理已平仓的档案
    foreach (var key in keysToRemove)
    {
        var profile = _positionProfiles[key];
        var triggerCount = profile.TriggerRecords.Count;
        
        if (triggerCount > 0)
        {
            _logger.LogInformation($"🗑️ 清理已平仓档案: {key} - 清理触发记录: {triggerCount}个");
        }
        
        // 记录清理历史
        var cleanupHistory = new ExecutionHistory
        {
            Symbol = profile.Symbol,
            PositionSide = profile.PositionSide,
            ExecutionType = "状态清理",
            ExecutionTime = DateTime.Now,
            Details = $"平仓后清理历史状态，共清理{triggerCount}个触发记录"
        };
        _executionHistory.Add(cleanupHistory);
    }
    
    // 🔧 新增：检测重新开仓的合约并清理历史状态
    var newPositionKeys = activeKeys.Where(k => !_positionProfiles.ContainsKey(k)).ToList();
    foreach (var newKey in newPositionKeys)
    {
        // 检查是否存在该合约的历史执行记录
        var keyParts = newKey.Split('_');
        if (keyParts.Length == 2)
        {
            var symbol = keyParts[0];
            var positionSide = keyParts[1];
            
            var historicalRecords = _executionHistory
                .Where(h => h.Symbol == symbol && h.PositionSide == positionSide && h.ExecutionType != "状态清理")
                .ToList();
            
            if (historicalRecords.Any())
            {
                _logger.LogInformation($"🔄 检测到重新开仓: {newKey} - 发现{historicalRecords.Count}条历史记录，准备清理");
                
                // 清理该合约的历史执行记录
                _executionHistory.RemoveAll(h => h.Symbol == symbol && h.PositionSide == positionSide && h.ExecutionType != "状态清理");
            }
        }
    }
}
```

#### 1.2 清理时机

系统会在以下两种情况下自动清理历史状态：

1. **发现合约已平仓**：持仓数量为0时，清理该合约的档案和触发记录
2. **发现重新开仓**：检测到新的持仓key，但存在历史执行记录时，自动清理历史记录

### 2. 手动清理功能

#### 2.1 单合约清理功能

```csharp
[RelayCommand]
private async Task ClearContractAutoMonitorHistoryAsync()
{
    // 清理指定合约的所有历史状态
    // 支持LONG和SHORT两个方向的清理
}
```

**使用方法**：
1. 选择要清理的合约
2. 点击"清理合约历史状态"按钮
3. 系统会检测是否有活跃持仓，并给出警告
4. 确认后清理该合约的所有触发记录和执行历史

#### 2.2 全部清理功能

```csharp
[RelayCommand]
private async Task ClearAllAutoMonitorHistoryAsync()
{
    // 清理所有合约的历史状态
    // 高风险操作，需要二次确认
}
```

**使用方法**：
1. 点击"清理所有历史状态"按钮（高风险操作）
2. 系统会给出警告，说明风险
3. 二次确认后清理所有合约的历史状态

### 3. 持久化服务增强

#### 3.1 新增清理方法

```csharp
/// <summary>
/// 清理特定合约的历史状态
/// </summary>
public void CleanupContractHistory(string symbol, string positionSide, string reason = "重新开仓")

/// <summary>
/// 批量清理多个合约的历史状态
/// </summary>
public void BatchCleanupContractHistory(List<(string symbol, string positionSide)> contracts, string reason = "批量清理")
```

#### 3.2 清理策略

- **保留清理记录**：清理操作本身会被记录，方便追踪
- **智能过滤**：只清理交易执行记录，保留状态清理记录
- **批量处理**：支持一次性清理多个合约的状态

## 功能特性

### 1. 自动化清理

- ✅ **自动检测平仓**：持仓为0时自动清理档案
- ✅ **自动检测重新开仓**：发现新持仓时清理历史记录
- ✅ **实时保存**：清理结果立即保存到持久化存储
- ✅ **详细日志**：记录每次清理的详细信息

### 2. 手动清理控制

- ✅ **单合约清理**：针对特定合约的精确清理
- ✅ **全部清理**：一键清理所有历史状态
- ✅ **安全确认**：多重确认防止误操作
- ✅ **活跃持仓检测**：清理前检查是否有活跃持仓

### 3. 状态监控增强

在"查看监控状态"中新增历史状态管理信息：
```
📋 历史状态管理:
   • 当前档案数: 3个，触发记录: 12个
   • 可能需要清理的旧档案: 1个

💡 重要说明:
   • 合约平仓后建议清理历史状态，避免重新开仓时重复触发
```

## 使用建议

### 1. 自动清理（推荐）

让系统自动处理，无需人工干预：
- 平仓时自动清理档案
- 重新开仓时自动清理历史记录
- 最安全、最便捷的方式

### 2. 手动清理的使用场景

#### 何时使用单合约清理
- 合约已完全平仓，但系统未及时清理
- 想要重置某个合约的所有触发状态
- 测试时需要清理特定合约的状态

#### 何时使用全部清理
- 程序长时间运行后，积累了大量历史状态
- 账户切换后需要全部重置
- 测试或调试时需要完全清空状态

### 3. 风险提示

⚠️ **手动清理的风险**：
- 清理后会重置所有触发状态
- 如果还有活跃持仓，可能导致重复触发
- 建议只在确认合约已平仓后使用

⚠️ **全部清理的风险**：
- 会影响所有合约，包括有活跃持仓的合约
- 可能导致当前持仓重复执行推仓/保本/止盈
- 建议只在所有合约都平仓后使用

## 技术实现要点

### 1. 防重复机制保持

清理功能不会影响现有的防重复机制：
- `TriggerRecords` 检查仍然有效
- 持久化状态恢复机制保持
- 冷却期机制继续工作

### 2. 清理记录可追踪

所有清理操作都会记录到执行历史：
```json
{
  "Symbol": "BTCUSDT",
  "PositionSide": "LONG", 
  "ExecutionType": "状态清理",
  "ExecutionTime": "2024-01-15T14:30:00",
  "Details": "平仓后清理历史状态，共清理3个触发记录"
}
```

### 3. 智能清理策略

- 保留清理类型的记录，方便审计
- 只清理实际的交易执行记录
- 支持按原因分类清理（平仓清理、手动清理等）

## 总结

此功能完美解决了用户提出的问题：
- ✅ 自动清理平仓后的历史状态
- ✅ 检测重新开仓并清理历史记录
- ✅ 提供手动清理的灵活控制
- ✅ 保持系统的安全性和可追踪性

用户现在可以放心使用自动盯盘功能，不用担心历史状态影响新开仓的操作。系统会智能地管理历史状态，确保每次开仓都能正常触发所有配置的阶梯。 