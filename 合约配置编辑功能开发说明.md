# 合约配置编辑功能开发说明

## 🎯 功能概述

在自动盯盘系统中新增了强大的合约配置编辑功能，允许用户直接修改触发金额、保盈金额和执行状态，而无需重新配置整个系统。

### 核心功能
- ✅ **触发金额修改**：支持修改保本、推仓、止盈条件的触发金额
- ✅ **保盈金额修改**：专门为止盈条件提供保盈金额编辑
- ✅ **状态重置**：将已执行的条件重置为未触发状态
- ✅ **合约启用控制**：启用或禁用特定合约的监控
- ✅ **实时保存**：所有修改立即保存到本地配置文件

## 🏗️ 技术实现

### 文件结构
```
Views/
├── ContractEditDialog.xaml          # 编辑对话框UI界面
├── ContractEditDialog.xaml.cs       # 编辑对话框后台逻辑
└── AutoMonitorDashboard.xaml.cs     # 更新了调用编辑对话框的逻辑
```

### 核心类和方法

#### 1. ContractEditDialog 类
**位置**: `Views/ContractEditDialog.xaml.cs`

**主要属性**:
- `ContractMonitorModel _contract`: 被编辑的合约配置
- `Dictionary<int, TriggerConditionEditData> _editData`: 编辑数据缓存
- `bool _hasChanges`: 是否有未保存的修改

**核心方法**:
```csharp
public ContractEditDialog(ContractMonitorModel contract, ILogger logger)
void CreateTriggerConditionEditors()  // 动态创建编辑器
void SaveChangesButton_Click()        // 保存修改
void ResetAllStatusButton_Click()     // 重置所有状态
```

#### 2. TriggerConditionEditData 类
**作用**: 存储编辑过程中的原始值和新值

**属性**:
```csharp
public int Id { get; set; }
public decimal OriginalTriggerPrice { get; set; }
public decimal NewTriggerPrice { get; set; }
public decimal OriginalKeepValue { get; set; }
public decimal NewKeepValue { get; set; }
public TriggerExecutionStatus OriginalStatus { get; set; }
public TriggerExecutionStatus NewStatus { get; set; }
```

### 界面设计特点

#### 1. 响应式布局
- 保本和推仓条件：显示 **2列**（信息、触发金额、状态）
- 止盈条件：显示 **3列**（信息、触发金额、保盈金额、状态）

#### 2. 输入验证
- 实时验证数值输入
- 无效输入显示红色背景
- 触发金额必须 > 0
- 保盈金额必须 >= 0

#### 3. 视觉反馈
- 不同类型条件使用不同颜色标识
- 按钮状态动态变化
- 修改状态实时提示

## 🔧 使用方法

### 1. 打开编辑对话框
```csharp
// 在AutoMonitorDashboard中调用
private bool ShowSimplifiedEditDialog(ContractMonitorModel contract)
{
    var editDialog = new ContractEditDialog(contract, _logger);
    editDialog.Owner = this;
    
    var result = editDialog.ShowDialog();
    return result == true && editDialog.HasChanges;
}
```

### 2. 用户操作流程
1. 在监控面板中点击合约的"编辑"按钮
2. 在弹出的编辑对话框中修改各项参数
3. 点击"保存修改"按钮应用更改
4. 修改立即保存到本地配置文件

### 3. 编辑界面布局
```
┌─────────────────────────────────────────────────┐
│ 📊 合约基本信息                                 │
│   合约代码: BTCUSDT    持仓方向: LONG           │
│   当前价格: 50,000.00  持仓数量: 0.001          │
│   未实现盈亏: +125.50 USDT  ☑️ 启用此合约监控  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 🎯 触发条件配置                                 │
│                                                │
│ 🛡️ 保本条件                                    │
│ ┌─────────────────────────────────────────────┐ │
│ │ 保本条件    │ 触发金额(USDT) │ 状态控制    │ │
│ │ 状态: 未触发 │ [50.00]        │ [🔄 重置]   │ │
│ └─────────────────────────────────────────────┘ │
│                                                │
│ 📈 推仓条件                                     │
│ ┌─────────────────────────────────────────────┐ │
│ │ 推仓第1档   │ 触发金额(USDT) │ 状态控制    │ │
│ │ 状态: 未触发 │ [100.00]       │ [🔄 重置]   │ │
│ └─────────────────────────────────────────────┘ │
│                                                │
│ 💰 止盈条件                                     │
│ ┌─────────────────────────────────────────────┐ │
│ │止盈第1档│触发金额(USDT)│保盈金额(USDT)│状态│ │
│ │状态:未触发│[500.00]     │[400.00]     │[🔄]│ │
│ └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘

        [🔄 重置所有状态] [✅ 保存修改] [❌ 取消]
```

## 📊 数据流程

### 1. 加载流程
```
AutoMonitorDashboard.EditConditionsButton_Click()
    ↓
ShowSimplifiedEditDialog()
    ↓
new ContractEditDialog(contract, logger)
    ↓
LoadContractData() + CreateTriggerConditionEditors()
```

### 2. 保存流程
```
SaveChangesButton_Click()
    ↓
ValidateInputs()
    ↓
ApplyChanges()
    ↓
SaveContractConfigsToFile()
    ↓
DialogResult = true
```

### 3. 数据验证
- 输入验证在 `TextBox.TextChanged` 事件中实时进行
- 最终验证在 `ValidateInputs()` 方法中统一处理
- 错误提示通过界面颜色变化和消息框显示

## 🔄 状态管理

### 触发条件状态
- `NotTriggered`: 未触发（绿色）
- `Executing`: 执行中（黄色）
- `Executed`: 已执行（红色）

### 状态重置逻辑
```csharp
if (editData.NewStatus == TriggerExecutionStatus.NotTriggered)
{
    condition.LastExecutionTime = null;
    condition.StatusNote = $"手动重置 {DateTime.Now:HH:mm:ss}";
}
```

## 🎨 界面设计原则

### 1. 用户体验
- 直观的视觉层次结构
- 清晰的功能分组
- 一致的操作反馈

### 2. 响应式设计
- 根据条件类型动态调整列数
- 输入验证实时反馈
- 按钮状态智能变化

### 3. 错误处理
- 友好的错误提示
- 异常情况的优雅处理
- 操作确认对话框

## 🔍 测试要点

### 1. 功能测试
- ✅ 触发金额修改是否生效
- ✅ 保盈金额修改是否生效
- ✅ 状态重置是否正常
- ✅ 批量重置是否工作
- ✅ 启用/禁用功能是否正常

### 2. 数据验证测试
- ✅ 无效输入是否被正确拦截
- ✅ 边界值处理是否正确
- ✅ 数据持久化是否正常

### 3. 界面测试
- ✅ 不同条件类型的界面布局
- ✅ 响应式设计在不同窗口大小下的表现
- ✅ 颜色和图标显示是否正确

## 🚀 性能优化

### 1. 内存管理
- 及时清理事件监听器
- 避免内存泄漏

### 2. 界面优化
- 动态创建界面元素
- 合理使用缓存机制

### 3. 数据处理
- 批量处理数据修改
- 优化文件IO操作

## 🔧 扩展可能性

### 1. 高级编辑功能
- 支持公式计算
- 批量编辑多个合约
- 条件模板功能

### 2. 可视化增强
- 图表显示触发条件
- 实时预览修改效果
- 历史修改记录

### 3. 集成增强
- 与其他模块的深度集成
- 外部配置导入导出
- 云端配置同步

## 📝 开发注意事项

1. **异常处理**: 所有用户输入都需要进行验证和异常处理
2. **数据一致性**: 确保修改操作的原子性
3. **用户体验**: 提供清晰的操作反馈和错误提示
4. **扩展性**: 代码结构便于后续功能扩展

## 🔍 故障排查

### 常见问题
1. **编辑按钮无响应**: 检查监控是否已停止
2. **对话框打开失败**: 检查日志文件确认错误原因
3. **修改不生效**: 检查配置文件权限和保存逻辑
4. **界面显示异常**: 检查数据格式和绑定逻辑

---

**开发完成时间**: 2025-01-01  
**版本**: v1.0.0  
**状态**: ✅ 已完成  
**测试状态**: 🧪 待测试 