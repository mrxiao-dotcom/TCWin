<Window x:Class="BinanceFuturesTrader.Views.OrderHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="订单历史记录" Height="600" Width="1200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" x:Name="TitleTextBlock" 
                      Text="订单历史记录" 
                      FontSize="16" FontWeight="Bold" 
                      VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock x:Name="CountTextBlock" 
                          Text="共 0 条记录" 
                          FontSize="12" 
                          VerticalAlignment="Center" 
                          Margin="0,0,10,0"/>
                <Button x:Name="RefreshButton" 
                        Content="刷新" 
                        Width="60" Height="25" 
                        Click="RefreshButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- 订单列表 -->
        <DataGrid Grid.Row="1" x:Name="OrderHistoryDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="LightGray"
                  FontSize="11">
            
            <DataGrid.Columns>
                <!-- 时间 -->
                <DataGridTextColumn Header="更新时间" 
                                   Binding="{Binding UpdateTime, StringFormat=MM-dd HH:mm:ss}" 
                                   Width="100" 
                                   SortDirection="Descending"/>
                
                <!-- 订单ID -->
                <DataGridTextColumn Header="订单ID" 
                                   Binding="{Binding OrderId}" 
                                   Width="80"/>
                
                <!-- 合约 -->
                <DataGridTextColumn Header="合约" 
                                   Binding="{Binding Symbol}" 
                                   Width="80"/>
                
                <!-- 方向 -->
                <DataGridTextColumn Header="方向" 
                                   Binding="{Binding Side}" 
                                   Width="50">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="BUY">
                                    <Setter Property="Foreground" Value="Green"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                                <Trigger Property="Text" Value="SELL">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- 订单类型 -->
                <DataGridTextColumn Header="类型" 
                                   Binding="{Binding Type}" 
                                   Width="80"/>
                
                <!-- 数量 -->
<DataGridTextColumn Header="数量" 
                                   Binding="{Binding OrigQty, StringFormat=F8}" 
                                   Width="100"/>
                
                <!-- 已成交数量 -->
                <DataGridTextColumn Header="已成交" 
                                   Binding="{Binding ExecutedQty, StringFormat=F8}" 
                                   Width="100"/>
                
                <!-- 价格 -->
                <DataGridTextColumn Header="价格" 
                                   Binding="{Binding Price, StringFormat=F8}" 
                                   Width="100"/>
                
                <!-- 触发价格 -->
                <DataGridTextColumn Header="触发价" 
                                   Binding="{Binding StopPrice, StringFormat=F8}" 
                                   Width="100"/>
                
                <!-- 状态 -->
                <DataGridTextColumn Header="状态" 
                                   Binding="{Binding Status}" 
                                   Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="FILLED">
                                    <Setter Property="Foreground" Value="Green"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                                <Trigger Property="Text" Value="PARTIALLY_FILLED">
                                    <Setter Property="Foreground" Value="Orange"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                                <Trigger Property="Text" Value="CANCELED">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </Trigger>
                                <Trigger Property="Text" Value="REJECTED">
                                    <Setter Property="Foreground" Value="Red"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- 持仓方向 -->
                <DataGridTextColumn Header="持仓方向" 
                                   Binding="{Binding PositionSide}" 
                                   Width="70"/>
                
                <!-- 减仓标记 -->
                <DataGridCheckBoxColumn Header="减仓" 
                                       Binding="{Binding ReduceOnly}" 
                                       Width="50"/>
                
                <!-- 下单时间 -->
                <DataGridTextColumn Header="下单时间" 
                                   Binding="{Binding Time, StringFormat=MM-dd HH:mm:ss}" 
                                   Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- 底部信息栏 -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock x:Name="StatisticsTextBlock" 
                          Text="统计信息将在这里显示" 
                          FontSize="12" 
                          VerticalAlignment="Center"/>
            </StackPanel>
            
            <Button Grid.Column="1" 
                    Content="关闭" 
                    Width="80" Height="30" 
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window> 