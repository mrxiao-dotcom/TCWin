<Window x:Class="BinanceFuturesTrader.Views.ClearAllConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="一键清仓确认" 
        Height="300" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#2D2D30">

    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="ConfirmButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#A71E2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A6268"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#495057"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题和警告图标 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="⚠️" FontSize="24" Margin="0,0,10,0"/>
            <TextBlock Text="一键清仓确认" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="#FFC107"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 确认内容 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="您即将执行一键清仓操作，此操作将会："
                       FontSize="14"
                       Foreground="White"
                       Margin="0,0,0,15"
                       TextWrapping="Wrap"/>

            <StackPanel Margin="20,0,0,0">
                <TextBlock Text="• 取消所有未成交的委托订单（包括止损、止盈等）"
                           FontSize="13"
                           Foreground="#FFE5B4"
                           Margin="0,0,0,5"
                           TextWrapping="Wrap"/>
                
                <TextBlock Text="• 平掉所有持仓（多头和空头）"
                           FontSize="13"
                           Foreground="#FFE5B4"
                           Margin="0,0,0,5"
                           TextWrapping="Wrap"/>
                
                <TextBlock Text="• 清空账户中的所有交易状态"
                           FontSize="13"
                           Foreground="#FFE5B4"
                           Margin="0,0,0,15"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <Border Background="#DC3545" 
                    CornerRadius="4" 
                    Padding="10"
                    Margin="0,10,0,0">
                <TextBlock Text="⚠️ 警告：此操作不可撤销，请确认您真的要清空所有持仓和订单！"
                           FontSize="13"
                           FontWeight="Bold"
                           Foreground="White"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Name="ConfirmButton"
                    Content="确认清仓"
                    Style="{StaticResource ConfirmButtonStyle}"
                    Margin="0,0,15,0"
                    Click="ConfirmButton_Click"/>
            
            <Button Name="CancelButton"
                    Content="取消"
                    Style="{StaticResource CancelButtonStyle}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 